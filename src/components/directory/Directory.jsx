import PropTypes from 'prop-types';
import React from 'react';
//import { Test } from './Directory.styles';

const Directory = props => <div className='DirectoryWrapper'>Test content</div>;

Directory.propTypes = {
	// bla: PropTypes.string,
};

Directory.defaultProps = {
	// bla: 'test',
};

export default Directory;
