import PropTypes from 'prop-types';
import React from 'react';
//import { Test } from './MenuItem.styles';

const MenuItem = props => <div className='MenuItemWrapper'>Test content</div>;

MenuItem.propTypes = {
	// bla: PropTypes.string,
};

MenuItem.defaultProps = {
	// bla: 'test',
};

export default MenuItem;
